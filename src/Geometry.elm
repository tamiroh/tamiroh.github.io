module Geometry exposing (Position, Screen, Vector)

-- GEOMETRY


type alias Position =
    ( Float, Float )


type alias Vector =
    ( Float, Float )


type alias Screen =
    { width : Float
    , height : Float
    }
